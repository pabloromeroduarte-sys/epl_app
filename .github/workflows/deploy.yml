name: Despliegue EPL App a Vultr

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar deploy en servidor
        run: |
          curl -f -s "https://padel.207.246.68.77.nip.io/deploy_webhook.php?token=${{ secrets.DEPLOY_TOKEN }}" \
            || curl -f -s -k "https://207.246.68.77/deploy_webhook.php?token=${{ secrets.DEPLOY_TOKEN }}"
