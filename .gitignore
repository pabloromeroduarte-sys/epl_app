*.zip
*.tar.gz
*.sql
/temp_archive/
/node_modules/
.env

# Archivos subidos por usuarios en producción — NUNCA tocar con git clean
/uploads/
/cache/
/logs/
sitemap.xml

# Claude Code — muchos ficheros aquí pueden colgar watchers del IDE (p. ej. Antigravity)
.claude/

# Archivos temporales/diagnóstico (prefijo _) — nunca subir
/_*.php
/_*.html
